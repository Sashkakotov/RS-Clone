const getBoardControlHtml = (title: string) => `
<div class="board-control">
    <h2 class="board-title">${title}</h2>
    <div class="invite-block">
      
    </div>
  </div>
`;

export default getBoardControlHtml;
